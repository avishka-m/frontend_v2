// Layout dimensions
export const LAYOUT_DIMENSIONS = {
  sidebarWidth: 256,
  sidebarCollapsedWidth: 80,
  headerHeight: 64,
  logoHeight: 64,
};

// Colors
export const COLORS = {
  primary: '#1890ff',
  textSecondary: '#666',
  borderLight: '#f0f0f0',
  white: '#fff',
};

// Shadows
export const SHADOWS = {
  sidebar: '2px 0 8px rgba(0,0,0,0.15)',
  header: '0 2px 8px rgba(0,0,0,0.06)',
  content: '0 1px 2px rgba(0,0,0,0.03)',
};

// Spacing
export const SPACING = {
  pageMargin: 24,
  contentPadding: 24,
  sidebarPadding: 16,
};

// Border radius
export const BORDER_RADIUS = {
  content: '8px',
};

// Transitions
export const TRANSITIONS = {
  layout: 'all 0.2s',
}; 